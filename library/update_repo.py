# Based on repo scoring and bool values, runs commands to change repo settings in line with requirements.

import repositories

# Step 1: Run check_repo

# Step 2: Construct the API request
# - construct a mutation/api request based on which elements register "false" in the repo object

## To-do: make an option to do this command by command so users can select which items they wish to remediate

# Step 3: Run the API request

# Step 4: Report the results