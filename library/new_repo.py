'''
Creates a new repository. If a remote upstream is provided, attempts to configure it with sGit reqs. If not, sets local reqs only. When a remote is added, user will then need to perform an update.
'''